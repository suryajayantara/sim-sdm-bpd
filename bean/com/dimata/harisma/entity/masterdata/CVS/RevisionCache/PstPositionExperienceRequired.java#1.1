/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.dimata.harisma.entity.masterdata;

import com.dimata.qdep.db.DBException;
import com.dimata.qdep.db.DBHandler;
import com.dimata.qdep.db.DBResultSet;
import com.dimata.qdep.db.I_DBInterface;
import com.dimata.qdep.db.I_DBType;
import com.dimata.qdep.entity.Entity;
import com.dimata.qdep.entity.I_PersintentExc;
import com.dimata.util.Command;
import com.dimata.util.lang.I_Language;
import java.sql.ResultSet;
import java.util.Vector;


/**
 *
 * @author Acer
 */
public class PstPositionExperienceRequired extends DBHandler implements I_DBInterface, I_DBType, I_PersintentExc, I_Language {

    public static final String TBL_POSITION_EXPERIENCE_REQUIRED = "hr_pos_experience_req";
    public static final int FLD_POS_EXPERIENCE_REQ_ID = 0;
    public static final int FLD_POSITION_ID = 1;
    public static final int FLD_EXPERIENCE_ID = 2;
    public static final int FLD_DURATION_MIN = 3;
    public static final int FLD_DURATION_RECOMMENDED = 4;
    public static final int FLD_NOTE = 5;
    public static String[] fieldNames = {
        "POS_EXPERIENCE_REQ_ID",
        "POSITION_ID",
        "EXPERIENCE_ID",
        "DURATION_MIN",
        "DURATION_RECOMMENDED",
        "NOTE"
    };
    public static int[] fieldTypes = {
        TYPE_LONG + TYPE_PK + TYPE_ID,
        TYPE_LONG,
        TYPE_LONG,
        TYPE_INT,
        TYPE_INT,
        TYPE_STRING
    };

    public PstPositionExperienceRequired() {
    }

    public PstPositionExperienceRequired(int i) throws DBException {
        super(new PstPositionExperienceRequired());
    }

    public PstPositionExperienceRequired(String sOid) throws DBException {
        super(new PstPositionExperienceRequired(0));
        if (!locate(sOid)) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        } else {
            return;
        }
    }

    public PstPositionExperienceRequired(long lOid) throws DBException {
        super(new PstPositionExperienceRequired(0));
        String sOid = "0";
        try {
            sOid = String.valueOf(lOid);
        } catch (Exception e) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        }
        if (!locate(sOid)) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        } else {
            return;
        }
    }

    public int getFieldSize() {
        return fieldNames.length;
    }

    public String getTableName() {
        return TBL_POSITION_EXPERIENCE_REQUIRED;
    }

    public String[] getFieldNames() {
        return fieldNames;
    }

    public int[] getFieldTypes() {
        return fieldTypes;
    }

    public String getPersistentName() {
        return new PstPositionExperienceRequired().getClass().getName();
    }

    public static PositionExperienceRequired fetchExc(long oid) throws DBException {
        try {
            PositionExperienceRequired entPositionExperienceRequired = new PositionExperienceRequired();
            PstPositionExperienceRequired pstPstPositionExperienceRequired = new PstPositionExperienceRequired(oid);
            entPositionExperienceRequired.setOID(oid);
            entPositionExperienceRequired.setPositionId(pstPstPositionExperienceRequired.getlong(FLD_POSITION_ID));
            entPositionExperienceRequired.setExperienceId(pstPstPositionExperienceRequired.getlong(FLD_EXPERIENCE_ID));
            entPositionExperienceRequired.setDurationMin(pstPstPositionExperienceRequired.getInt(FLD_DURATION_MIN));
            entPositionExperienceRequired.setDurationRecommended(pstPstPositionExperienceRequired.getInt(FLD_DURATION_RECOMMENDED));
            entPositionExperienceRequired.setNote(pstPstPositionExperienceRequired.getString(FLD_NOTE));
            return entPositionExperienceRequired;
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstPositionExperienceRequired(0), DBException.UNKNOWN);
        }
    }

    public long fetchExc(Entity entity) throws Exception {
        PositionExperienceRequired entPositionExperienceRequired = fetchExc(entity.getOID());
        entity = (Entity) entPositionExperienceRequired;
        return entPositionExperienceRequired.getOID();
    }

    public long insertExc(Entity ent) throws Exception {
        return insertExc((PositionExperienceRequired) ent);
    }

    public long updateExc(Entity ent) throws Exception {
        return updateExc((PositionExperienceRequired) ent);
    }

    public long deleteExc(Entity ent) throws Exception {
        if (ent == null) {
            throw new DBException(this, DBException.RECORD_NOT_FOUND);
        }
        return deleteExc(ent.getOID());
    }

    public static long insertExc(PositionExperienceRequired entPositionExperienceRequired) throws DBException {
        try {
            PstPositionExperienceRequired pstPositionExperienceRequired = new PstPositionExperienceRequired(0);
            pstPositionExperienceRequired.setLong(FLD_POSITION_ID, entPositionExperienceRequired.getPositionId());
            pstPositionExperienceRequired.setLong(FLD_EXPERIENCE_ID, entPositionExperienceRequired.getExperienceId());
            pstPositionExperienceRequired.setInt(FLD_DURATION_MIN, entPositionExperienceRequired.getDurationMin());
            pstPositionExperienceRequired.setInt(FLD_DURATION_RECOMMENDED, entPositionExperienceRequired.getDurationRecommended());
            pstPositionExperienceRequired.setString(FLD_NOTE, entPositionExperienceRequired.getNote());

            pstPositionExperienceRequired.insert();
            entPositionExperienceRequired.setOID(pstPositionExperienceRequired.getLong(FLD_POS_EXPERIENCE_REQ_ID));
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstPositionExperienceRequired(0), DBException.UNKNOWN);
        }
        return entPositionExperienceRequired.getOID();
    }

    public static long updateExc(PositionExperienceRequired entPositionExperienceRequired) throws DBException {
        try {
            if (entPositionExperienceRequired.getOID() != 0) {
                PstPositionExperienceRequired pstPositionExperienceRequired = new PstPositionExperienceRequired(entPositionExperienceRequired.getOID());
                pstPositionExperienceRequired.setLong(FLD_POSITION_ID, entPositionExperienceRequired.getPositionId());
                pstPositionExperienceRequired.setLong(FLD_EXPERIENCE_ID, entPositionExperienceRequired.getExperienceId());
                pstPositionExperienceRequired.setInt(FLD_DURATION_MIN, entPositionExperienceRequired.getDurationMin());
                pstPositionExperienceRequired.setInt(FLD_DURATION_RECOMMENDED, entPositionExperienceRequired.getDurationRecommended());
                pstPositionExperienceRequired.setString(FLD_NOTE, entPositionExperienceRequired.getNote());

                pstPositionExperienceRequired.update();
                return entPositionExperienceRequired.getOID();

            }
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstPositionExperienceRequired(0), DBException.UNKNOWN);
        }
        return 0;
    }

    public static long deleteExc(long oid) throws DBException {
        try {
            PstPositionExperienceRequired pstPositionExperienceRequired = new PstPositionExperienceRequired(oid);
            pstPositionExperienceRequired.delete();
        } catch (DBException dbe) {
            throw dbe;
        } catch (Exception e) {
            throw new DBException(new PstPositionExperienceRequired(0), DBException.UNKNOWN);
        }
        return oid;
    }

    public static Vector listAll() {
        return list(0, 500, "", "");
    }

    public static Vector list(int limitStart, int recordToGet, String whereClause, String order) {
        Vector lists = new Vector();
        DBResultSet dbrs = null;
        try {
            String sql = "SELECT * FROM " + TBL_POSITION_EXPERIENCE_REQUIRED;
            if (whereClause != null && whereClause.length() > 0) {
                sql = sql + " WHERE " + whereClause;
            }
            if (order != null && order.length() > 0) {
                sql = sql + " ORDER BY " + order;
            }
            if (limitStart == 0 && recordToGet == 0) {
                sql = sql + "";
            } else {
                sql = sql + " LIMIT " + limitStart + "," + recordToGet;
            }
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            while (rs.next()) {
                PositionExperienceRequired entPositionExperienceRequired = new PositionExperienceRequired();
                resultToObject(rs, entPositionExperienceRequired);
                lists.add(entPositionExperienceRequired);
            }
            rs.close();
            return lists;

        } catch (Exception e) {
            System.out.println(e);
        } finally {
            DBResultSet.close(dbrs);
        }
        return new Vector();
    }

    public static Vector listInnerJoin(String where){
        Vector lists = new Vector();
        DBResultSet dbrs = null;
        try {
            String sql  = "SELECT * FROM " + TBL_POSITION_EXPERIENCE_REQUIRED;
                   sql += " INNER JOIN "+PstPosition.TBL_HR_POSITION+" ON "+TBL_POSITION_EXPERIENCE_REQUIRED+
                   "."+PstPositionExperienceRequired.fieldNames[PstPositionExperienceRequired.FLD_EXPERIENCE_ID]+"="+PstPosition.TBL_HR_POSITION+"."+PstPosition.fieldNames[PstPosition.FLD_POSITION_ID];
                   sql += " WHERE "+TBL_POSITION_EXPERIENCE_REQUIRED+"."+PstPositionExperienceRequired.fieldNames[PstPositionExperienceRequired.FLD_POSITION_ID]+" = "+where+"";
            
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            while (rs.next()) {
                Vector vect = new Vector(1,1);
                PositionExperienceRequired entPositionExperienceRequired = new PositionExperienceRequired();
                Position pos = new Position();
                resultToObject(rs, entPositionExperienceRequired);
                vect.add(entPositionExperienceRequired);
                pos.setPosition(rs.getString(PstPosition.fieldNames[PstPosition.FLD_POSITION]));
                vect.add(pos);
                lists.add(vect);
            }
            rs.close();
            return lists;

        } catch (Exception e) {
            System.out.println(e);
        } finally {
            DBResultSet.close(dbrs);
        }
        return lists;
    }
    
    public static Vector listInnerJoinVer1(String where){
        Vector lists = new Vector();
        DBResultSet dbrs = null;
        try {
            String sql  = "SELECT * FROM " + TBL_POSITION_EXPERIENCE_REQUIRED;
                   sql += " INNER JOIN hr_position POS ON "+TBL_POSITION_EXPERIENCE_REQUIRED+".POSITION_ID=POS.POSITION_ID ";
                   sql += " INNER JOIN "+PstPosition.TBL_HR_POSITION+" ON "+TBL_POSITION_EXPERIENCE_REQUIRED+
                   "."+PstPositionExperienceRequired.fieldNames[PstPositionExperienceRequired.FLD_POSITION_ID]+"="+PstPosition.TBL_HR_POSITION+"."+PstPosition.fieldNames[PstPosition.FLD_POSITION_ID];
                   sql += " WHERE "+TBL_POSITION_EXPERIENCE_REQUIRED+"."+PstPositionExperienceRequired.fieldNames[PstPositionExperienceRequired.FLD_POS_EXPERIENCE_REQ_ID]+" = "+where+"";
            
            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();
            while (rs.next()) {
                Vector vect = new Vector(1,1);
                PositionExperienceRequired entPositionExperienceRequired = new PositionExperienceRequired();
                resultToObject(rs, entPositionExperienceRequired);
                vect.add(entPositionExperienceRequired);
                // Position
                Position pos = new Position();
                pos.setPosition(rs.getString(PstPosition.fieldNames[PstPosition.FLD_POSITION]));
                vect.add(pos);
                // Education
                Position posReq = new Position();
                posReq.setPosition(rs.getString(PstPosition.fieldNames[PstPosition.FLD_POSITION]));
                vect.add(posReq);
                lists.add(vect);
            }
            rs.close();
            return lists;

        } catch (Exception e) {
            System.out.println(e);
        } finally {
            DBResultSet.close(dbrs);
        }
        return lists;
    }
    
    private static void resultToObject(ResultSet rs, PositionExperienceRequired entPositionExperienceRequired) {
        try {
            entPositionExperienceRequired.setOID(rs.getLong(PstPositionExperienceRequired.fieldNames[PstPositionExperienceRequired.FLD_POS_EXPERIENCE_REQ_ID]));
            entPositionExperienceRequired.setPositionId(rs.getLong(PstPositionExperienceRequired.fieldNames[PstPositionExperienceRequired.FLD_POSITION_ID]));
            entPositionExperienceRequired.setExperienceId(rs.getLong(PstPositionExperienceRequired.fieldNames[PstPositionExperienceRequired.FLD_EXPERIENCE_ID]));
            entPositionExperienceRequired.setDurationMin(rs.getInt(PstPositionExperienceRequired.fieldNames[PstPositionExperienceRequired.FLD_DURATION_MIN]));
            entPositionExperienceRequired.setDurationRecommended(rs.getInt(PstPositionExperienceRequired.fieldNames[PstPositionExperienceRequired.FLD_DURATION_RECOMMENDED]));
            entPositionExperienceRequired.setNote(rs.getString(PstPositionExperienceRequired.fieldNames[PstPositionExperienceRequired.FLD_NOTE]));


        } catch (Exception e) {
        }
    }

    public static boolean checkOID(long entPositionExperienceRequired) {
        DBResultSet dbrs = null;
        boolean result = false;
        try {
            String sql = "SELECT * FROM " + TBL_POSITION_EXPERIENCE_REQUIRED + " WHERE "
                    + PstPositionExperienceRequired.fieldNames[PstPositionExperienceRequired.FLD_POS_EXPERIENCE_REQ_ID] + " = " + entPositionExperienceRequired;

            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();

            while (rs.next()) {
                result = true;
            }
            rs.close();
        } catch (Exception e) {
            System.out.println("err : " + e.toString());
        } finally {
            DBResultSet.close(dbrs);
            return result;
        }
    }

    public static int getCount(String whereClause) {
        DBResultSet dbrs = null;
        try {
            String sql = "SELECT COUNT(" + PstPositionExperienceRequired.fieldNames[PstPositionExperienceRequired.FLD_POS_EXPERIENCE_REQ_ID] + ") FROM " + TBL_POSITION_EXPERIENCE_REQUIRED;
            if (whereClause != null && whereClause.length() > 0) {
                sql = sql + " WHERE " + whereClause;
            }

            dbrs = DBHandler.execQueryResult(sql);
            ResultSet rs = dbrs.getResultSet();

            int count = 0;
            while (rs.next()) {
                count = rs.getInt(1);
            }

            rs.close();
            return count;
        } catch (Exception e) {
            return 0;
        } finally {
            DBResultSet.close(dbrs);
        }
    }


    /* This method used to find current data */
    public static int findLimitStart(long oid, int recordToGet, String whereClause, String orderClause) {
        int size = getCount(whereClause);
        int start = 0;
        boolean found = false;
        for (int i = 0; (i < size) && !found; i = i + recordToGet) {
            Vector list = list(i, recordToGet, whereClause, orderClause);
            start = i;
            if (list.size() > 0) {
                for (int ls = 0; ls < list.size(); ls++) {
                    PositionExperienceRequired entPositionExperienceRequired = (PositionExperienceRequired) list.get(ls);
                    if (oid == entPositionExperienceRequired.getOID()) {
                        found = true;
                    }
                }
            }
        }
        if ((start >= size) && (size > 0)) {
            start = start - recordToGet;
        }

        return start;
    }
    /* This method used to find command where current data */

    public static int findLimitCommand(int start, int recordToGet, int vectSize) {
        int cmd = Command.LIST;
        int mdl = vectSize % recordToGet;
        vectSize = vectSize + (recordToGet - mdl);
        if (start == 0) {
            cmd = Command.FIRST;
        } else {
            if (start == (vectSize - recordToGet)) {
                cmd = Command.LAST;
            } else {
                start = start + recordToGet;
                if (start <= (vectSize - recordToGet)) {
                    cmd = Command.NEXT;
                    System.out.println("next.......................");
                } else {
                    start = start - recordToGet;
                    if (start > 0) {
                        cmd = Command.PREV;
                        System.out.println("prev.......................");
                    }
                }
            }
        }

        return cmd;
    }
}
